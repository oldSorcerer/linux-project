#!/bin/bash

# Используем команду `lsblk` для получения необходимой информации.
# Флаг `-o` позволяет указать столбцы для вывода, `NAME,FSTYPE,SIZE,MOUNTPOINT` предоставляет информацию о
# названиях дисков/разделов, файловой системе, размере и точке монтирования.
# Флаг `-a` для отображения всей структуры.
???